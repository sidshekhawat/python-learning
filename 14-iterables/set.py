"""
Iterables = An object/collection that can return its elements one at a time, allowing it to be iterated over in a loop

"""

fruits = {"apple", "banana", "coconut"}

for fruit in fruits:
    print(fruit)

#for fruit in reversed(fruits):   #'set' object is not reversible
    #print(fruit)